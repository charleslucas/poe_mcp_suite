---
name: gem-socket-analysis
description: Analyze a PoE character's socket colours, links, gem-to-socket binding, off-colours, and empty sockets via the authoritative PoE-API tool (get_socketed_gems) — the socket dimension PoB discards on import.
when_to_use: Use for skill-gem/socket questions — "full skill-gem analysis", "what's the best gem for this empty socket", "are there gem-arrangement improvements", "are any gems off-colour", "how hard is this item to colour", or checking sockets before a gear purchase. Skip a single gem lookup (get_gem_detail) and pure DPS-value gem simming (build-optimization-sim).
---

This task uses the **Skill-Gem & Socket Analysis** playbook. The playbooks in `playbooks/` are the single source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, context/pre-flight protocol, league reference, trust hierarchy, narration norms).
2. Then read `playbooks/gem-socket-analysis.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
