---
name: snapshot-driven-build-guide
description: Author a living build guide for a PoE character with no external guide (friend-built, theorycrafted, or heavily adapted) via dated PoB snapshots; produces a reusable template.
when_to_use: Use to snapshot a character and accrete a guide as it levels, track a build's evolution, or document a guideless/original build's progression. Skip one-off "import and check my stats" (just import) and comparing finished builds (build-comparison).
---

This task uses the **Snapshot-Driven Build Guide** playbook. The playbooks in `playbooks/` are the single
source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, pre-flight /
   context / character-snapshot / scope-filter protocol, league reference, trust hierarchy, narration norms,
   sub-agent criteria).
2. Then read `playbooks/snapshot-driven-build-guide.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
