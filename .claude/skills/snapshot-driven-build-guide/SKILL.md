---
name: snapshot-driven-build-guide
description: Author a build guide for a Path of Exile character that has no external guide (friend-built, theorycrafted, or heavily adapted) by importing it from PoE, saving dated PoB snapshots, and accreting a living guide as it levels — plus a reusable template for future characters.
when_to_use: Use when the user wants to snapshot a character and build a guide as they go, track a build's evolution across levels, document a friend-built/original build with no guide, or make a reusable template from a character. Also when importing a character that has no guides/ entry and the user wants its progression recorded. Skip for a one-off "import and check my stats" (just import) or comparing two finished builds (use build-comparison).
---

This task uses the **Snapshot-Driven Build Guide** playbook. The playbooks in `playbooks/` are the single
source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, pre-flight /
   context / character-snapshot / scope-filter protocol, league reference, trust hierarchy, narration norms,
   sub-agent criteria).
2. Then read `playbooks/snapshot-driven-build-guide.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
