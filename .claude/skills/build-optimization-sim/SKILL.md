---
name: build-optimization-sim
description: Systematic build optimization via live PoB simulation — finds the best gem swaps, support gem quality, and jewel re-rolls by actually simming each change before any currency is spent. Covers the full audit surface (links, quality, jewels, stat layers). Use when the user wants to know where to spend currency, which gems to upgrade, or whether a specific change is worth it.
when_to_use: Use when the user asks about optimizing a current build, which supports to use, whether gems are worth upgrading, which jewels to re-roll, or where a specific currency budget best moves the needle. Skip for a single quick stat lookup or a one-off price check.
---

This task uses the **Build Optimization via PoB Sim** playbook. The playbooks in `playbooks/` are the single source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, context/pre-flight protocol, league reference, trust hierarchy, narration norms).
2. Then read `playbooks/build-optimization-sim.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
