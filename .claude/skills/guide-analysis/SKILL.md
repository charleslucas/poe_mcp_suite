---
name: guide-analysis
description: Digest an external Path of Exile build guide (YouTube, written site, or pobb.in link) into the global guide library at character_data/guides/{archetype}/ — sub-agent transcript digestion, structured JSON entry, archetype README and synthesis updates.
when_to_use: Use when the user shares a build guide to analyze or add to the library — "analyze this guide", "add this build guide", "here's a YouTube build video", "what does this guide do", a guide URL with intent to study it, or multi-guide synthesis for an archetype. Skip for comparing a guide against the user's own character (use build-comparison), community consensus on an archetype (use community-survey), and quick "what item does this guide use" lookups (fetch the description or PoB link directly).
---

This task uses the **Guide Analysis** playbook. The playbooks in `playbooks/` are the single source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, context/pre-flight protocol, sub-agent patterns, narration norms).
2. Then read `playbooks/guide-analysis.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
