---
name: guide-analysis
description: Digest an external PoE build guide (YouTube, written site, or pobb.in link) into the guide library at character_data/guides/{archetype}/ — structured JSON entry, archetype README + synthesis updates.
when_to_use: Use when the user shares a build guide to analyze or add to the library — "analyze this guide", "add this build guide", "here's a YouTube build video", a guide URL with intent to study it, or multi-guide synthesis for an archetype. Skip comparing a guide to the user's own character (build-comparison), community consensus (community-survey), and quick single-fact guide lookups (fetch the description/PoB link directly).
---

This task uses the **Guide Analysis** playbook. The playbooks in `playbooks/` are the single source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, context/pre-flight protocol, sub-agent patterns, narration norms).
2. Then read `playbooks/guide-analysis.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
