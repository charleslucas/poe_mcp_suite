---
name: tree-analysis
description: Detailed passive-tree audit or reallocation — wasted nodes, reaching a target notable, freeing points — via node classification, connectivity checks, jewel-socket safety, mastery topology.
when_to_use: Use for substantive tree work — "look at my passive tree", "what nodes should I change", "I want to get [notable]", "can I free up points", "best passive point to take", "I just levelled up", "show me node power". Skip single-node lookups (get_tree_node). Verify path cost with find_path_to_node before recommending from get_passive_upgrades/get_node_power — they ignore travel cost.
---

This task uses the **Passive Tree Analysis** playbook. The playbooks in `playbooks/` are the single source of truth — read them rather than working from memory, so you always get the current version.

1. Read `playbooks/README.md` first — the shared meta-framework (cursory-vs-detailed gate, context/pre-flight protocol, league reference, trust hierarchy, narration norms).
2. Then read `playbooks/tree-analysis.md` and follow it for this task.

Do not summarize or duplicate the playbook content here.
